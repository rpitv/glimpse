mutation CreatePersonImage($personId: BigInt!, $imageId: BigInt!, $priority: Int!) {
  personImage: createPersonImage(input: { personId: $personId, imageId: $imageId, priority: $priority }) {
    id
  }
}
