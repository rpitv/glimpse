query FindAllProductions($pagination: PaginationInput) {
  productions: findManyProduction(pagination: $pagination) {
    id
    name
    startTime
    description
    thumbnail {
      path
    }
  }
}
