query FindLiveProductions($now: DateTime!) {
  findManyProduction(filter:{
    startTime:{
      lt: $now
    }
    endTime: {
      gt: $now
    }
  }, order:[
    { field: startTime, direction: Asc}
  ], pagination:{
    take:3
  }
  ) {
    id
    name
    description
    videos (order: [{
      field: priority, direction: Desc
    }]){
      priority
      video {
        format
        metadata
      }
    }
    startTime
  }
}
