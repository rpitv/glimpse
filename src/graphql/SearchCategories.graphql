query SearchCategories($pagination: PaginationInput, $filter: FilterCategoryInput) {
  categoryCount(filter: $filter)
  categories: findManyCategory(pagination: $pagination, filter: $filter) {
    id
    name
  }
}
