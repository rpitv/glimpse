query FindRecentProductions($paginationInput: PaginationInput!) {
  findManyProduction (order: [
    {
      field: startTime,
      direction: Desc
    },
  ],
  pagination: $paginationInput
  ) {
    id
    startTime
    name
    description
  }
}
