query FindRoles($pagination: PaginationInput, $filter: FilterRoleInput) {
  roleCount(filter: $filter)
  roles: findManyRole(pagination: $pagination, order: [{ field: priority, direction: Desc }, { field: name, direction: Desc }], filter: $filter) {
    id
    name
    priority
  }
}
